<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.no2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/chuicon"
        android:label="CHU體適能檢測換算"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".getdata"></activity>
        <activity android:name=".register" />
        <activity android:name=".answer" />
        <activity android:name=".no2p4" />
        <activity android:name=".no2p2" />
        <activity android:name=".no2p3" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
</manifest>